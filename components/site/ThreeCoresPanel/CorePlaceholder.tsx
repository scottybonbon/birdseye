/**
 * CorePlaceholder, ARCHIVED.
 *
 * The minimal label-only placeholder used during the holding state
 * between the iso-vignette revert and the abstract system schematic.
 * The schematics now live in GateScene / SafeScene / YardScene.
 *
 * Kept as a no-op stub so any stale import surfaces as a type error
 * rather than a runtime crash. Delete this file once a future cleanup
 * pass touches the panel folder.
 */
export function CorePlaceholder(_: { label: string }) {
  return null;
}
